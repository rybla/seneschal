## Fix app layout to fit window

Refactor CSS and layout styles in src/pages/index.css and src/pages/index.tsx to ensure the app fits within the window (100vw/100vh) without global scrollbars. Implement flex/grid layout for sidebar and main content areas to scroll independently.

