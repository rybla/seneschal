## Fix Graph Visibility

Fixed an issue where graph nodes and edges were invisible on the dark background by providing high-contrast explicit colors to the canvas renderer. Also ensured the graph only initializes when its container has valid dimensions to guarantee correct centering.

