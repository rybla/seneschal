## Expand Knowledge Graph

Modified the Knowledge Graph Explorer to dynamically resize the force graph to fill the available height. Updated CSS to use flexbox for layout, and implemented a React hook to measure and adapt the graph size.

