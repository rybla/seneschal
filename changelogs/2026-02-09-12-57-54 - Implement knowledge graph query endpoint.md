## Implement knowledge graph query endpoint

Added a new /api/query endpoint to handle natural language queries against the knowledge graph. This includes Gemini integration for entity extraction and database helpers for graph traversal.

