## Dynamic Graph Polling

Implemented polling in SearchSection to fetch graph data every 5 seconds. Added mergeGraphData logic to update the graph visualization without triggering a full re-layout, preserving node positions and simulation state.

